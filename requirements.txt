# Core runtime for HASI mainline experiments.
# Install the PyTorch wheel that matches the remote CUDA/CPU environment first
# if the server requires a custom index URL, then install this file.

torch>=2.1
torch-geometric>=2.5
ogb>=1.3.6
numpy>=1.24
networkx>=3.0
PyYAML>=6.0
scikit-learn>=1.3
